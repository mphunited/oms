# Archived Migrations

These files were applied directly to the database via Supabase MCP
(required for tables outside the original tablesFilter, or for auth-schema DDL).
They are captured in snapshot 0014 and retained for historical reference only.
They are NOT tracked in _journal.json and should NOT be re-applied.
